services:
  - type: web
    name: akansu-loading-plan
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: PDF_PASSWORD
        value: akansu
